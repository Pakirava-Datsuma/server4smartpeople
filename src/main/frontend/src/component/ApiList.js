import {ApiController} from './ApiController';


export const HouseController =
  new ApiController ({
    prefix: "/places",
  });
export var UserController =
  new ApiController ({
    prefix: "/users",
  });
